<h1><%= @user.username %>'s Profile</h1>

<h2>Your Orders:</h2>
<% @user.orders.each do |order| %>
  <p>Order <%= link_to order.id, user_order_path(@user, order) %></p>
<% end %>

<%= link_to 'Create a New Order', new_user_order_path(@user), class: "btn btn-primary" %>
